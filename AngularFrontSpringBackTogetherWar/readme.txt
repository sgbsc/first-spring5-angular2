To build the Angular project, go to the project (named 'frontend'), and compile and build it ahead of time (--aot)

C:\AngularFrontSpringBackTogetherWar\frontend>ng build --aot

Run the Spring application as a Spring Boot App in STS

Go to localhost:8080/index.html to see the front page :)